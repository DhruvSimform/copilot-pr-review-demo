[project]
name = "todo-fastapi"
version = "0.1.0"
description = "A simple todo API POC with in-memory storage"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
]
